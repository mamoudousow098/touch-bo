import { CustomCurrencyFrPipePipe } from './custom-currency-fr-pipe.pipe';

describe('CustomCurrencyFrPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomCurrencyFrPipePipe();
    expect(pipe).toBeTruthy();
  });
});
