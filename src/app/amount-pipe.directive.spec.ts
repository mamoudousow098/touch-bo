import { AmountPipeDirective } from './amount-pipe.directive';

describe('AmountPipeDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountPipeDirective();
    expect(directive).toBeTruthy();
  });
});
